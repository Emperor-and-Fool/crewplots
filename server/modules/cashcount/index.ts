import { Router } from 'express';

// FUTURE: Import cashcount routes when implemented
// import cashcountRoutes from './routes';

const router = Router();

// FUTURE: Mount cashcount routes
// router.use('/', cashcountRoutes);

export default router;