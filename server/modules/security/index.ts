import { Router } from 'express';

// FUTURE: Import security routes when implemented
// import securityRoutes from './routes';

const router = Router();

// FUTURE: Mount security routes
// router.use('/', securityRoutes);

export default router;