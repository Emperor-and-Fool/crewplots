# CrewPlots Landing Page

This directory contains assets and components specific to the CrewPlots marketing landing page.

## Future Architecture

This landing page will become a separate website with its own deployment when the product launches. Each customer will receive their own isolated CrewPlots environment via docker-compose Infrastructure as Code (IaC).

## Structure

- `assets/` - Landing page specific images and media
- Future: Separate React app, separate domain, separate deployment

## Assets

- `homepage-bg.png` - Production team background image for Benefits section