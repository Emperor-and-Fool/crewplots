// Auth Module - Compatibility Layer Only
// All functionality migrated to users module, this serves as compatibility re-export

// Compatibility re-exports for gradual migration
export { useAuth } from '@/hooks/use-auth';